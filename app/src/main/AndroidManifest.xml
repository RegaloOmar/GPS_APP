<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rr.gps_app">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ConsultaFechas.FechaActivity"></activity>
        <activity
            android:name=".ConsultaFechas.ConsultaFechaActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EnrrampeActivity"
            android:parentActivityName=".CamaraActivity" />
        <activity
            android:name=".CamaraActivity"
            android:parentActivityName=".SemaforoActivity" />
        <activity
            android:name=".IncidenciasActivity"
            android:parentActivityName=".SemaforoActivity" />
        <activity
            android:name=".DatosActivity"
            android:parentActivityName=".TalonActivity" />
        <activity
            android:name=".TalonActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SemaforoActivity"
            android:parentActivityName=".DatosActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TransportistaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="{$applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>